name: loomhub
server: bp.example.com
port: 8080
domains:
  - loomhub.example.com
build:
  dockerfile: Dockerfile
  context: .
volumes:
  - loomhub-data:/app/data
env:
  DATABASE_URL: postgres://user:password@db-host:5432/dbname?sslmode=disable
